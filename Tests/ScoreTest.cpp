#include <pch.h>
#include "gtest/gtest.h"
#include <Score.h>
#include <wx/filefn.h>


TEST(ScoreTest, ScoreTest){
//    Score score(0);
//    ASSERT_EQ(0, score.GetScore());
//    score.AddScore(1);
//    ASSERT_EQ(1, score.GetScore());
//    score.AddScore(11);
//    ASSERT_EQ(12, score.GetScore());
//    score.Reset();
//    ASSERT_EQ(0, score.GetScore());
}